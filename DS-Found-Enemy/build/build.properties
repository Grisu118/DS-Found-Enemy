name = DS-Found-Enemy
version = 0.1

# directory settings (input)
src.dir   = ${basedir}/src/prod
test.dir  = ${basedir}/src/test
res.dir   = ${basedir}/res
lib.dir   = ${basedir}/lib
build.dir = ${basedir}/build
l4j.dir = ${build.dir}/l4j

#directory settings to external libs and tools needed for the build
build.lib.dir = ${build.dir}/lib

# directory settings (output)
bin.dir   = ${basedir}/bin
log.dir   = ${basedir}/log
doc.dir   = ${basedir}/doc

bin.classes.dir = ${bin.dir}/classes
bin.jar.dir    = ${bin.dir}/jar
log.report.dir = ${log.dir}/report
log.report.test.dir = ${log.report.dir}/test
log.report.checkstyle.dir = ${log.report.dir}/checkstyle
log.jacoco.dir = ${log.report.dir}/jacoco
doc.api.dir   = ${doc.dir}/api

launch4j.dir.server = /var/launch4j

# main class
main.class = ch.grisu118.diestaemme.foundenemy.business.Main_DSFoundEnemy